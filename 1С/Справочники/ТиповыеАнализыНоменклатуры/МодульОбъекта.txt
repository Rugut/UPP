Процедура ПередЗаписью(Отказ)
	
	Если НЕ ОбменДанными.Загрузка И НЕ ЭтоГруппа Тогда
		
		Для Каждого СтрокаТаб Из ПоказателиАнализа Цикл
			Если НЕ ЗначениеЗаполнено(СтрокаТаб.ПоказательАнализа) Тогда
				ОбщегоНазначения.СообщитьОбОшибке( "Не указан показатель анализа (строка № " + СтрокаТаб.НомерСтроки + ")", Отказ);
			КонецЕсли;
		КонецЦикла;
		
	КонецЕсли;
	
КонецПроцедуры // ПередЗаписью()