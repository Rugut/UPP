Перем СерыйЦвет;

Процедура СправочникДеревоПриПолученииДанных(Элемент, ОформленияСтрок)
	
	Для Каждого ОформлениеСтроки Из ОформленияСтрок Цикл
		Если ОформлениеСтроки.ДанныеСтроки = Неопределено Тогда
			ОформлениеСтроки.Ячейки.Код.УстановитьТекст("");
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

Процедура ДействияФормыЗагрузитьКлассификатор(Кнопка)
	
	Справочники.НалоговыеОрганы.ПолучитьФорму("ФормаЗагрузкиКлассификатора").Открыть();
	
КонецПроцедуры

Процедура ДействияФормыДерево(Кнопка)
	
	ЭлементыФормы.ДействияФормы.Кнопки.Дерево.Пометка = НЕ ЭлементыФормы.ДействияФормы.Кнопки.Дерево.Пометка;
	УправлениеОтображениемДерева();
	
КонецПроцедуры

Процедура УправлениеОтображениемДерева()
	
	ЭлементыФормы.СправочникДерево.Дерево = ЭлементыФормы.ДействияФормы.Кнопки.Дерево.Пометка;
	ЭлементыФормы.СправочникДерево.Колонки.Код.ОтображатьИерархию = ЭлементыФормы.ДействияФормы.Кнопки.Дерево.Пометка;
	ЭлементыФормы.СправочникДерево.ИерархическийПросмотр = ЭлементыФормы.ДействияФормы.Кнопки.Дерево.Пометка;
	
КонецПроцедуры

Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	
	Если Не РежимВыбора Тогда
		ЭлементыФормы.ДействияФормы.Кнопки.Удалить(0);
		ЭлементыФормы.ДействияФормы.Кнопки.Удалить(0);
	КонецЕсли;
	
	ОтображатьВВидеДерева = ВосстановитьЗначение("СправочникНалоговыеОрганы_ОтображениеВВидеДерева");
	ЭлементыФормы.ДействияФормы.Кнопки.Дерево.Пометка = ?(ОтображатьВВидеДерева = Неопределено, Ложь, ОтображатьВВидеДерева);
	УправлениеОтображениемДерева();
	
КонецПроцедуры

Процедура ПриЗакрытии()
	
	СохранитьЗначение("СправочникНалоговыеОрганы_ОтображениеВВидеДерева", ЭлементыФормы.ДействияФормы.Кнопки.Дерево.Пометка);
	СохранитьЗначение("СправочникНалоговыеОрганы_ПолеСортировки", СправочникСписок.Порядок);
	
КонецПроцедуры

Процедура ДействияФормыВидыНалоговыхОрганов(Кнопка)
	
	Справочники.ВидыНалоговыхОрганов.ПолучитьФормуСписка().Открыть();
	
КонецПроцедуры

СерыйЦвет = Новый Цвет(160, 160, 160);