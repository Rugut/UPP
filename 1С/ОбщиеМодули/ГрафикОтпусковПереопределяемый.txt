Функция ПолучитьТекстЗапросаГрафикаОтпусков() Экспорт
	
	Возврат "ВЫБРАТЬ
	|	ПланированиеОтпуска.Сотрудник.Физлицо.Наименование КАК ФИО,
	|	ПланированиеОтпуска.Сотрудник КАК Сотрудник,
	|	ПланированиеОтпуска.Сотрудник.Физлицо КАК Физлицо,
	|	ПланированиеОтпуска.ДатаНачала КАК ДатаНачала,
	|	ПланированиеОтпуска.ДатаОкончания,
	|	ВЫБОР
	|		КОГДА ПланированиеОтпуска.Ссылка.Проведен
	|				И ПланированиеОтпуска.СостояниеУтверждения = ЗНАЧЕНИЕ(Перечисление.СостоянияУтверждения.Утвержден)
	|			ТОГДА ИСТИНА
	|		ИНАЧЕ ЛОЖЬ
	|	КОНЕЦ КАК Утвержден,
	|	ПланированиеОтпуска.Ссылка КАК Регистратор
	|ИЗ
	|	Документ.ПланированиеОтпуска.Работники КАК ПланированиеОтпуска
	|ГДЕ
	|	(НЕ ПланированиеОтпуска.Ссылка.ПометкаУдаления)
	|	И ПланированиеОтпуска.ДатаОкончания >= &НачалоПериода
	|	И ПланированиеОтпуска.ДатаНачала <= &КонецПериода
	|	И ПланированиеОтпуска.Сотрудник.Физлицо В (&МассивФизлиц)
	|
	|УПОРЯДОЧИТЬ ПО
	|	ФИО,
	|	ДатаНачала";
	
КонецФункции

#Если ТолстыйКлиентОбычноеПриложение Тогда
Процедура ФормаОбработкиЗарегистрироватьФактическийОтпуск() Экспорт
	
	Форма = Документы.ОтсутствияНаРабочемМесте.ПолучитьФормуНовогоДокумента();
	Форма.ПричинаОтсутствия	= Справочники.ПричиныОтсутствияНаРаботе.ЕжегодныйОтпуск;
	Форма.ДатаНачала		= НачалоДня(ОбщегоНазначения.ПолучитьРабочуюДату());
	Форма.Датаокончания		= НачалоДня(ОбщегоНазначения.ПолучитьРабочуюДату());
	
	Форма.Открыть();
	
КонецПроцедуры
#КонецЕсли

Функция ПолучитьРеквизитУпорядочиванияФизическихЛиц() Экспорт
	
	Возврат "Наименование";
	
КонецФункции

Процедура ПередЗаписьюПланированиеОтпускаДополнительно(Работники) Экспорт
	
	// В этой конфигурации дополнительных действий не предусмотрено
	
КонецПроцедуры

Процедура ОбработкаЗаполненияПланированиеОтпускаДополнительно(Объект, ТипОснования, Основание, Работники) Экспорт
	
	// В этой конфигурации дополнительных действий не предусмотрено
	
КонецПроцедуры

Процедура ДополнитьЗапросПоРаботники(Запрос, Объект) Экспорт
	
	// В этой конфигурации дополнительных действий не предусмотрено
	
КонецПроцедуры

Процедура ПроверитьЗаполнениеСтрокиРаботникаДополнительно(ВыборкаПоШапкеДокумента, ВыборкаПоСтрокамДокумента, Отказ, Заголовок) Экспорт
	
	// В этой конфигурации дополнительных действий не предусмотрено
	
КонецПроцедуры

Процедура ТекстЗапросаАвтозаполненияДополнительно(СтрокаЗапроса) Экспорт
	
	// В этой конфигурации дополнительных действий не предусмотрено
	
КонецПроцедуры