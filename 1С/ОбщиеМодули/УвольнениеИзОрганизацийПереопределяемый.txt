////////////////////////////////////////////////////////////////////////////////
// Вспомогательные процедуры, функции

////////////////////////////////////////////////////////////////////////////////
// Процедуры, функции объекта

Процедура ДополнитьСтруктуруПечатныхФорм(СтруктураПечатныхФорм, ДокументОбъект) Экспорт
	
	УвольнениеИзОрганизацийДополнительный.ДополнитьСтруктуруПечатныхФорм(СтруктураПечатныхФорм, ДокументОбъект);
	
КонецПроцедуры

Функция ПечатьДополнительныхФорм(ИмяМакета, Объект) Экспорт
	
	Возврат УвольнениеИзОрганизацийДополнительный.ПечатьДополнительныхФорм(ИмяМакета, Объект);
	
КонецФункции

Процедура ОбработкаЗаполненияДополнительно(ТипОснования, ЭтотОбъект, Основание) Экспорт
	
	Если ТипОснования = Тип("ДокументСсылка.Увольнение") Тогда
		
		// Заполним реквизиты из стандартного набора.
		ЗаполнениеДокументов.ЗаполнитьШапкуДокументаПоОснованию(ЭтотОбъект, Основание);

		Если Основание.Проведен Тогда
			
			ТекущийПользователь = глЗначениеПеременной("глТекущийПользователь");
			
			ЭтотОбъект.РаботникиОрганизации.Загрузить(УвольнениеИзОрганизацийДополнительный.СформироватьЗапросОбработкаЗаполненияПоДокументамУвольнение(ЭтотОбъект, Основание).Выгрузить());
		
		КонецЕсли;
		
	КонецЕсли;
	
КонецПроцедуры

Процедура ПроверитьЗаполнениеСтрокиРаботникаОрганизацииДополнительно(ВыборкаПоСтрокамДокумента, СтрокаНачалаСообщенияОбОшибке, Отказ, Заголовок) Экспорт
	
	// В этой конфигурации дополнительных действий не предусмотрено
	
КонецПроцедуры

// Возвращает текст поля запроса
//
// Параметры
//  нет
//
// Возвращаемое значение:
//   строка
//
Функция ДополнительноеПолеОписанияСотрудника() Экспорт 
	
	Возврат	"НЕОПРЕДЕЛЕНО КАК ДополнительноеПолеПроверки"

КонецФункции // ПолучитьПоле()

Процедура ДобавитьДополнительныеДвижения(ДокументОбъект, Отказ, Заголовок) Экспорт
	
	УвольнениеИзОрганизацийДополнительный.ДобавитьДополнительныеДвижения(ДокументОбъект, Отказ, Заголовок);
	
КонецПроцедуры

////////////////////////////////////////////////////////////////////////////////
// Процедуры, функции для работы формы документа

Процедура ДополнительноУточнитьДниКомпенсации(СтрокаДанных, СведенияОСотруднике) Экспорт

	// В этой конфигурации дополнительных действий не предусмотрено

КонецПроцедуры

#Если ТолстыйКлиентОбычноеПриложение Тогда

Процедура ДополнитьКнопкиКоманднойПанели(КоманднаяПанель, Действие) Экспорт
	
	ИспользоватьУправленческийУчетЗарплаты = глЗначениеПеременной("глИспользоватьУправленческийУчетЗарплаты");
	
	Если ИспользоватьУправленческийУчетЗарплаты Тогда
		
		КнопкаЗаполнитьУволенными = КоманднаяПанель.Кнопки.Добавить("ЗаполнитьУволенными", ТипКнопкиКоманднойПанели.Действие, "Уволенными по предприятию", Действие);
		КнопкаЗаполнитьУволенными.Подсказка = "Заполнить уволенными";
		
	КонецЕсли;

КонецПроцедуры

Процедура ВыполнитьДополнительныеДействия(ДокументОбъект, ФормаДокумента) Экспорт
	
	УвольнениеИзОрганизацийДополнительный.ЗаполнитьТабличнуюЧастьРаботникиОрганизацииУволенные(ДокументОбъект, ФормаДокумента);
		
КонецПроцедуры

Процедура УточнитьОформлениеСтрокиСотрудника(ОформлениеСтроки, СведенияОСотруднике) Экспорт

	// В этой конфигурации дополнительных действий не предусмотрено

КонецПроцедуры

Процедура ФормаДокументаПередОткрытиемДополнительно(Форма) Экспорт
	
	УвольнениеИзОрганизацийДополнительный.ФормаДокументаПередОткрытиемДополнительно(Форма)
	
КонецПроцедуры

#КонецЕсли