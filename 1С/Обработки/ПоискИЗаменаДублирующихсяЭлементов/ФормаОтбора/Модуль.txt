Процедура ЗначенияОтбораПриВыводеСтроки(Элемент, ОформлениеСтроки, ДанныеСтроки)
	Если ДанныеСтроки.ИндексКартинки <> -1 Тогда
		ОформлениеСтроки.Ячейки.Представление.ИндексКартинки = ДанныеСтроки.ИндексКартинки;	
	КонецЕсли;
КонецПроцедуры

Процедура ОсновныеДействияФормыКнопкаОК(Кнопка)
	ВыбратьЗначение();
КонецПроцедуры

Процедура ВыбратьЗначение()
	СтуктураВыбора = Новый Структура;
	СтуктураВыбора.Вставить("Значение", ЭлементыФормы.ЗначенияОтбора.ТекущиеДанные.Значение);
	СтуктураВыбора.Вставить("Представление", ЭлементыФормы.ЗначенияОтбора.ТекущиеДанные.Представление);
	ОповеститьОВыборе(СтуктураВыбора);	
КонецПроцедуры

Процедура ЗначенияОтбораПередНачаломИзменения(Элемент, Отказ)
	Отказ = Истина;
	ВыбратьЗначение();
КонецПроцедуры

Процедура ПриОткрытии()
	Если НачальноеЗначение <> Неопределено Тогда
		СтрокаТаблицы = ЗначенияОтбора.Найти(НачальноеЗначение , "Значение");
		Если СтрокаТаблицы<>Неопределено Тогда
			ЭлементыФормы.ЗначенияОтбора.ТекущаяСтрока = СтрокаТаблицы;
		КонецЕсли;
	КонецЕсли;
КонецПроцедуры