Перем ПоказыватьПоОрганизациям;

Процедура ОбновитьСписок()
	
	ЭлементыФормы.ДействияФормы.Кнопки.ПоказыватьПоОрганизациям.Пометка = ПоказыватьПоОрганизациям;
	РаботаСДиалогамиЗК.ИзменитьВидимостьПанелиПоГоризонтали(ЭтаФорма, ПоказыватьПоОрганизациям, "ПанельУдержанияУпр", "УдержанияРегл");
	
КонецПроцедуры

///////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ФОРМЫ

Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	
	// ПоказыватьПоОрганизациям
	ПоказыватьПоОрганизациям = ВосстановитьЗначение("ФормаУдержанийПоказыватьПоОрганизациям");
	Если ПоказыватьПоОрганизациям = Неопределено Тогда
		ПоказыватьПоОрганизациям = Истина;
	КонецЕсли;
	
	ОбновитьСписок();
	
	ЭлементыФормы.РамкаГруппыУдержанияУпр.Заголовок = "Удержания " + ОбщегоНазначения.ПреобразоватьСтрокуИнтерфейса("предприятия");

КонецПроцедуры

Процедура ПередЗакрытием(Отказ, СтандартнаяОбработка)
	
	СохранитьЗначение("ФормаУдержанийПоказыватьПоОрганизациям", ПоказыватьПоОрганизациям);

КонецПроцедуры

///////////////////////////////////////////////////////////////////////////////
// ОБРАБОТЧИКИ СОБЫТИЙ ЭЛЕМНТОВ ФОРМЫ

Процедура ДействияФормыПоказыватьПоОрганизациям(Кнопка)
	
	ПоказыватьПоОрганизациям = Не ПоказыватьПоОрганизациям;
	
	ОбновитьСписок();
	
КонецПроцедуры