Процедура ОКНажатие(Кнопка)
	
	Если НЕ ЗначениеЗаполнено(ИмяПользователя) Тогда
		Предупреждение("Не задано имя пользователя!");
		ТекущийЭлемент = ЭлементыФормы.ИмяПользователя;
		Возврат;
	КонецЕсли;
	
	Закрыть(Новый Структура("ИмяПользователя, Пароль", ИмяПользователя, Пароль));
	
КонецПроцедуры