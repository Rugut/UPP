Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	
	ЭлементыФормы.ПолеHTMLДокументаСодержание.Перейти(КонтекстЭДО.Метаданные().Макеты.ПредупреждениеПриПереходеНа693);
	
	ПодключитьОбработчикОжидания("ОбработчикОжидания", 0.1, Истина);
	
КонецПроцедуры

Процедура ПолеHTMLДокументаСодержаниеonclick(Элемент, pEvtObj)
	
	htmlElement = pEvtObj.srcElement;

	Пока htmlElement <> Неопределено И ВРег(htmlElement.tagName) <> "A" Цикл
		htmlElement = htmlElement.parentElement;
	КонецЦикла;

	Если htmlElement = Неопределено Тогда
		Возврат;
	КонецЕсли;

	ТекстСсылки = htmlElement.href;
	
	Если Найти(ТекстСсылки, "Приказ534") <> 0 Тогда
		pEvtObj.returnValue = Ложь;
		ЗапуститьПриложение("http://www.nalog.ru/document_light.php?id=27615&topic=imns_1");
	КонецЕсли;
	
КонецПроцедуры

Процедура ОбработчикОжидания()
	
	Активизировать();
	
КонецПроцедуры

Процедура ОсновныеДействияФормыЗакрыть(Кнопка)
	
	Закрыть();
	
КонецПроцедуры

Процедура ПриЗакрытии()
	
	СохранитьЗначение("КонтекстЭДО_НеПоказыватьПредупреждение534", БольшеНеПоказыватьЭтоОкно);
	
КонецПроцедуры