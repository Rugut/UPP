Процедура ОсновныеДействияФормыКнопкаОК(Кнопка)
	
	Если ПустаяСтрока(Фамилия) Тогда
		Сообщить("Не указана фамилия сотрудника.",СтатусСообщения.Важное);
		Возврат;
	КонецЕсли;
	
	Если ПустаяСтрока(Имя) Тогда
		Сообщить("Не указано имя сотрудника.",СтатусСообщения.Важное);
		Возврат;
	КонецЕсли;
	
	ЭтаФорма.ВладелецФормы.ВладелецЭЦПФамилия	 = Фамилия;
	ЭтаФорма.ВладелецФормы.ВладелецЭЦПИмя		 = Имя;
	ЭтаФорма.ВладелецФормы.ВладелецЭЦПОтчество	 = Отчество;
	Закрыть();
КонецПроцедуры