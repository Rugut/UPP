Процедура ПередОткрытием(Отказ, СтандартнаяОбработка)
	
	Если Не РежимВыбора Тогда
		ЭлементыФормы.ДействияФормы.Кнопки.Удалить(0);
		ЭлементыФормы.ДействияФормы.Кнопки.Удалить(0);
	КонецЕсли;
	
КонецПроцедуры

Процедура ДействияФормыНастройкиАвтообмена(Кнопка)
	
	РегистрыСведений.НастройкиПользователейУчетныхЗаписейДокументооборота.ПолучитьФормуСписка().Открыть();
	
КонецПроцедуры

Процедура ДействияФормыДобавитьТакском(Кнопка)
	
	КонтекстЭДО.ПолучитьФорму("РОКИМастерАвтонастройкиНовойУчетнойЗаписи").Открыть();
	
КонецПроцедуры

Процедура ОбработкаОповещения(ИмяСобытия, Параметр, Источник)
	
	Если ИмяСобытия = "Опрос форм" И Параметр.Ключ = "Справочник_УчетныеЗаписиДокументооборота_ФормаСписка" Тогда
		Параметр.Форма = ЭтаФорма;
	КонецЕсли;
	
КонецПроцедуры

Процедура ДействияФормыЗаявления(Кнопка)
	ФормаСписка = Документы.ЗаявлениеАбонентаСпецоператораСвязи.ПолучитьФормуСписка("ФормаСписка");
	ФормаСписка.Открыть();
КонецПроцедуры

Процедура ЗаявлениеНаПодключениеРучное(Кнопка)
	
	ФормаНовогоЗаявления = Документы.ЗаявлениеАбонентаСпецоператораСвязи.ПолучитьФормуНовогоДокумента("ФормаДокумента");
	ФормаНовогоЗаявления.ДополнительныеСвойства.Вставить("РучноеЗаполнение", Истина);
	ФормаНовогоЗаявления.Открыть();
	
КонецПроцедуры

Процедура ЗаявлениеНаПодключениеМастер(Кнопка)
	
	Документы.ЗаявлениеАбонентаСпецоператораСвязи.ПолучитьФормуНовогоДокумента("ФормаДокумента").Открыть();
	
КонецПроцедуры

Процедура ДействияФормыДобавитьУниверсальнаяСхема(Кнопка)
	// Вставить содержимое обработчика.
	КонтекстЭДО.ПолучитьФорму("ФормаУниверсальнаяАвтонастройкаУчетнойЗаписи").Открыть();
	
КонецПроцедуры