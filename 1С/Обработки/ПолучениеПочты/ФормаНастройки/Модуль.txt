Процедура ОсновныеДействияФормыОК(Элемент)
	
	ПараметрВозврата = Новый Структура;

	ПараметрВозврата.Вставить("ВидПолученияПисем", ВидПолученияПисем);
	ПараметрВозврата.Вставить("ПрочитанностьПолучаемыхПисем", ПрочитанностьПолучаемыхПисем);

	ЭтаФорма.Закрыть(ПараметрВозврата);
	
КонецПроцедуры