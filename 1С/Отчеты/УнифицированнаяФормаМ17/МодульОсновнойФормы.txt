Процедура КоманднаяПанель1Сформировать(Кнопка)

	ОбновитьОтчет(ЭлементыФормы.ТабДокумент);

КонецПроцедуры // КоманднаяПанель1Сформировать()

Процедура МатериалОчистка(Элемент, СтандартнаяОбработка)

	СтандартнаяОбработка = Ложь;
	
КонецПроцедуры // МатериалОчистка()

Процедура ОрганизацияОчистка(Элемент, СтандартнаяОбработка)

	СтандартнаяОбработка = Ложь;
	
КонецПроцедуры // ОрганизацияОчистка()

Процедура ПриОткрытии()

	Если Не ЗначениеЗаполнено(Организация) Тогда
		Организация = УправлениеПользователями.ПолучитьЗначениеПоУмолчанию( глЗначениеПеременной("глТекущийПользователь"), "ОсновнаяОрганизация");
	КонецЕсли;
	Если Не ЗначениеЗаполнено(Склад) Тогда
		Склад = УправлениеПользователями.ПолучитьЗначениеПоУмолчанию( глЗначениеПеременной("глТекущийПользователь"), "ОсновнойСклад");
	КонецЕсли;
	Если Не ЗначениеЗаполнено(КонДата) Тогда
		НачДата = НачалоМесяца(КонДата);	
	КонецЕсли;
	Если Не ЗначениеЗаполнено(КонДата) Тогда
		КонДата = ТекущаяДата();
	КонецЕсли;
	Если Не ЗначениеЗаполнено(НачДата) Тогда
		НачДата = НачалоМесяца(КонДата);
	КонецЕсли;

КонецПроцедуры // ПриОткрытии()

Процедура КнопкаВыборПериодаНажатие(Элемент)

	Период = Новый НастройкаПериода();
	Период.ВариантНастройки = ВариантНастройкиПериода.Интервал;
	Период.ДатаНачала       = НачДата;
	Период.ДатаОкончания    = КонДата;
	Если Период.Редактировать() Тогда
		НачДата = Период.ПолучитьДатуНачала();
		КонДата = Период.ПолучитьДатуОкончания();
	КонецЕсли;

КонецПроцедуры // КнопкаВыборПериодаНажатие()