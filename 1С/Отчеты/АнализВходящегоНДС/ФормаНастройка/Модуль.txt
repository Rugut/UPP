Перем мОрганизация;

Перем мДатаНач;

Перем мДатаКон;

Перем мСравнитьНДСВключенныйВСтоимость;
Перем мСравнитьНДСПредъявленныйПоставщиком;
Перем мСравнитьНДСПредъявленныйКВычету;

////////////////////////////////////////////////////////////////////////////////
// ПРОЦЕДУРЫ - ДЕЙСТВИЯ КОМАНДНЫХ ПАНЕЛЕЙ ФОРМЫ

Процедура КнопкаНастройкаПериодаНажатие(Элемент)

	НП.УстановитьПериод(ДатаНач, ДатаКон);

	Если НП.Редактировать() Тогда

		ДатаНач = НП.ПолучитьДатуНачала();
		ДатаКон = НП.ПолучитьДатуОкончания();

	КонецЕсли;

КонецПроцедуры // КнопкаНастройкаПериодаНажатие()

Процедура ОсновныеДействияФормыДействиеОК(Кнопка)
	
	Закрыть();
	
КонецПроцедуры

Процедура ОсновныеДействияФормыДействиеОтмена(Кнопка)
	
	Организация = мОрганизация;
	ДатаНач = мДатаНач;
	ДатаКон = мДатаКон;
	СравнитьНДСВключенныйВСтоимость = мСравнитьНДСВключенныйВСтоимость;
	СравнитьНДСПредъявленныйПоставщиком = мСравнитьНДСПредъявленныйПоставщиком;
	СравнитьНДСПредъявленныйКВычету = мСравнитьНДСПредъявленныйКВычету;
	Закрыть();
	
КонецПроцедуры

Процедура ПриОткрытии()
	
	мОрганизация = Организация;
	мДатаНач = ДатаНач;
	мДатаКон = ДатаКон;
	мСравнитьНДСВключенныйВСтоимость = СравнитьНДСВключенныйВСтоимость;
	
КонецПроцедуры