Процедура ПриОткрытии()
	
	ТекущийПользователь = глЗначениеПеременной("глТекущийПользователь");
	
	Если НЕ ЗначениеЗаполнено(Организация) Тогда
		Организация 	= УправлениеПользователями.ПолучитьЗначениеПоУмолчанию(ТекущийПользователь, "ОсновнаяОрганизация");
	КонецЕсли;
	Если НЕ ЗначениеЗаполнено(Склад) Тогда
		Склад     		= УправлениеПользователями.ПолучитьЗначениеПоУмолчанию(ТекущийПользователь, "ОсновнойСклад");
	КонецЕсли;
	
	УстановитьВидимостьДоступность();
	
КонецПроцедуры // ПриОткрытии()


Процедура ВыводитьДанныеВКолонкуПриИзменении(Элемент)
	
	УстановитьВидимостьДоступность();
	
КонецПроцедуры

Процедура УстановитьВидимостьДоступность()
	
	Для НомерКолонки = 1 По мКоличествоКолонок Цикл
		
		КолонкаИспользуется = ЭтотОбъект["ВыводитьДанныеВКолонку" + СокрЛП(НомерКолонки)];
		ПолеДатыКолонки		= ЭлементыФормы["Дата" + СокрЛП(НомерКолонки)];
		
		ПолеДатыКолонки.АвтоОтметкаНезаполненного = КолонкаИспользуется;
		ПолеДатыКолонки.Доступность 			  = КолонкаИспользуется;
		
	КонецЦикла;
	
КонецПроцедуры


Процедура ТипЦенНачалоВыбора(Элемент, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	
	ФормаВыбора = Справочники.ТипыЦенНоменклатуры.ПолучитьФормуВыбора(,Элемент);
	ФормаВыбора.Отбор.Рассчитывается.Установить(Ложь);
	ФормаВыбора.НачальноеЗначениеВыбора = ТипЦен;
	ФормаВыбора.Открыть();

КонецПроцедуры

Процедура КоманднаяПанель1Сформировать(Кнопка)

	ОбновитьОтчет(ЭлементыФормы.ТабДокумент);

КонецПроцедуры // КоманднаяПанель1Сформировать()