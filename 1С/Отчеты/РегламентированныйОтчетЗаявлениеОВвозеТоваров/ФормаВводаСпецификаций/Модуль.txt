Процедура ОсновныеДействияФормыОК(Кнопка)
	
	//избавляемся от пустых строк
	//сворачиваем
	ТЗСпецификаций.Свернуть("Дата, Номер");
	//удаляем последнюю
	Для каждого Строка Из ТЗСпецификаций Цикл
		Если НЕ ЗначениеЗаполнено(Строка.Дата) И НЕ ЗначениеЗаполнено(Строка.Номер) Тогда
	    	ТЗСпецификаций.Удалить(Строка);
			Прервать;
		КонецЕсли;
	КонецЦикла;
		
	
	//флОК = Истина;
	//Для Сч = 1 По ТЗСпецификаций.Количество() Цикл
	//	Строка = ТЗСпецификаций[Сч - 1];
	//	
	//	Если НЕ ЗначениеЗаполнено(Строка.Дата) Тогда
	//		Сообщить("В строке " + Сч + " не заполнена дата спецификации.");	
	//		флОК = Ложь;
	//	КонецЕсли;
	//	
	//	Если НЕ ЗначениеЗаполнено(Строка.Номер) Тогда
	//		Сообщить("В строке " + Сч + " не заполнен номер спецификации.");	
	//		флОК = Ложь;
	//	КонецЕсли;
	//	
	//КонецЦикла; 
	//
	//Если НЕ флОК Тогда
	//	Возврат;
	//КонецЕсли;
	
	Закрыть(ТЗСпецификаций); 	
	
КонецПроцедуры


Процедура ПриОткрытии()
	
	ЭлементыФормы.ТЗСпецификаций.СоздатьКолонки();
	
КонецПроцедуры