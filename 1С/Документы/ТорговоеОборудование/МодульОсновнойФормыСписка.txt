///////////////////////////////////////////////////////////////////////////////
//// ОБРАБОТЧИКИ СОБЫТИЙ

// Процедура представляет обработчик события "Нажатие" кнопки "СоздатьИзСписка".
//
// Параметры
//  Кнопка - <КнопкаКоманднойПанели>
//         - Кнопка, с которой связано данное событие (кнопка "СоздатьИзСписка").
//
Процедура ДействияФормыСоздатьИзСписка(Кнопка)

	Форма = Справочники.ТорговоеОборудование.ПолучитьФорму(
	    "ФормаСпискаПоддерживаемогоОборудования",
	    ЭтаФорма,
	    ЭтаФорма);
	Если Форма.Открыта() Тогда
		Форма.Активизировать();
	Иначе
		Форма.Открыть();
	КонецЕсли;

КонецПроцедуры // ДействияФормыСоздатьИзСписка()