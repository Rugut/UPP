Перем СохраненныеНастройки;

Функция ПолучитьИмяСохраненнойНастройки()

	Возврат "НастройкаФормаВыбораКассыРКО";

КонецФункции // ПолучитьИмяСохраненнойНастройки()

Процедура ОсновныеДействияФормыОК(Кнопка)
	
	Закрыть(Истина);
	
КонецПроцедуры

Процедура ПриОткрытии()
	
	СохраненныеНастройки = ВосстановитьЗначение(ПолучитьИмяСохраненнойНастройки());
	Если СохраненныеНастройки = Неопределено Тогда
		СохраненныеНастройки = Новый Соответствие;
	КонецЕсли;
	
	Касса = СохраненныеНастройки[Организация];

КонецПроцедуры

Процедура ПриЗакрытии()
	
	СохранитьНастройки();
	
КонецПроцедуры

Процедура СохранитьНастройки()

	Если ЗначениеЗаполнено(Касса) Тогда
		
		СохраненныеНастройки.Вставить(Организация, Касса);
		
		СохранитьЗначение(ПолучитьИмяСохраненнойНастройки(), СохраненныеНастройки);
		
	КонецЕсли;

КонецПроцедуры // СохранитьНастройки()